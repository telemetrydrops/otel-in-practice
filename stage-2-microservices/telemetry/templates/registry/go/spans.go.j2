{# spans.go.j2 #}
// Code generated by weaver. DO NOT EDIT.

package {{ params.package_name }}

// Span names from the ecommerce semantic convention registry.
const (
{% for group in ctx %}
{% for span in group.spans %}
    {{ span.brief | comment(format="go") }}
    {# Read the runtime name from annotations: Weaver v0.22.1 resolves `name` from the dotted type, not from `name.note`. The verb-object string lives in annotations.runtime_name (see spans.yaml). #}
    Span{{ span.id | replace("span.", "") | pascal_case_const }}Name = "{{ span.annotations.runtime_name }}"
{% endfor %}
{% endfor %}
)
